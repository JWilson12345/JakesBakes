# Jake's Bakes Customer Site

Open with a local server from this folder:

```powershell
C:\Users\Jacob\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m http.server 8091
```

Then visit `http://localhost:8091`.

Prices live in `assets/js/config.js`: `fourPackPrice: 11.99` and `sixPackPrice: 15.99`.
